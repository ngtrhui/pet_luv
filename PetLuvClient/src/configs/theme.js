import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#f79400',
      dark: '#d87a00',
      main: '#f79400',
    },
    secondary: {
      light: '#0e1826',
      dark: '#09111b',
      main: '#0e1826',
    },
    tertiary: {
      light: '#cfcfcf',
      dark: '#aeaeae',
    },
  },
});

export default theme;
